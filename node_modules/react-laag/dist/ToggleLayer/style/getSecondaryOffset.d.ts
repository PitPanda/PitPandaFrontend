import { Rects, AnchorEnum } from "../types";
export default function findSecondaryOffset(anchor: AnchorEnum, anchorOptions: AnchorEnum[], rects: Rects, triggerOffset: number, scrollOffset: number): number;
