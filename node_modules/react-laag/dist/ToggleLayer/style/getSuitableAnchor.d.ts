import { Rects, AnchorEnum, LayerDimensions } from "../types";
export default function findBestSuitableAnchor(rects: Rects, anchorOptions: AnchorEnum[], triggerOffset: number, scrollOffset: number, layerDimensions: LayerDimensions | null): AnchorEnum;
